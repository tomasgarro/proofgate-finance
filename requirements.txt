financetoolkit>=1.9.0
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.26.0
requests>=2.31.0
anthropic>=0.40.0
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-cov>=4.0.0

# SEC EDGAR data (free, no API key required)
edgartools>=3.0.0

# Dune Analytics Python SDK (optional — our client uses urllib directly,
# but the SDK adds retry logic and typed query builders)
# dune-client>=1.7.0

# DefiLlama and Xerberus clients use urllib (stdlib) — no extra packages needed
